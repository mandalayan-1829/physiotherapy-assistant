# AI Physio — Frontend Environment Variables

# API
VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000

# Mock mode — set to "true" to use mock data instead of real backend
VITE_USE_MOCK_API=true
